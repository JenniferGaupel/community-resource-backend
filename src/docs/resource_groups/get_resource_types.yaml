Get resource types associated with a resource group
---
tags:
  - Resource Groups

parameters:
  - in: query
    description: The id of the resource group to retrieve resource type details for
    name: id
    required: true    

responses:
  200:
    description: All resource types associated with a single resource group

  404:
    description: Resource group not found for resource group id
